const { EmbedBuilder, ButtonBuilder } = require("discord.js");

module.exports = {
    name: "nome do comando",
    description: "Descrissão do comando",
    aliases: ["agli1", "ali2"],
    execute(message, args) {

        // Logica do codigo

    }
}